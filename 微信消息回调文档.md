# 微信消息回调接口文档

> 微信消息后，会把消息**回调（POST）**到指定的 `hook_url`。
> 本服务默认监听端口：`10878`（一般无需调整），回调地址：`hook_url`（均可在 `weixin_config.txt` 中配置）。

---

## 一、回调机制

- 服务启动后，持续监听微信消息；
- 收到消息后，通过 **HTTP POST** 发送到 `hook_url`（默认 `http://localhost:8080/stock/wechat/hook`）；
- 请求头：`Content-Type: application/json`；
- **只回调"收到的消息"**，自己发送的消息不回调。

---

## 二、回调参数（POST JSON）

收到一条消息时，POST 到 `hook_url` 的 JSON 结构如下：

```json
{
  "fromUser": "wxid_xxxx 或 xxxx@chatroom",
  "msgId": 1234567890123456,
  "content": "消息文本内容",
  "nickname": "发送者昵称",
  "wxid": "消息发送者自身wxid",
  "type": 1
}
```

### 字段说明

| 字段 | 类型 | 说明 |
|------|------|------|
| `fromUser` | string | 消息来源。个人消息为发送者的 wxid（`wxid_xxx`）；群消息为群 ID（`xxx@chatroom`） |
| `msgId` | int | 消息唯一 ID（可用于 `forwardMsg` 转发） |
| `content` | string | 消息文本内容 |
| `nickname` | string | 发送者昵称。群消息时为群内成员昵称；个人消息时为对方昵称 |
| `wxid` | string | 当前登录账号（机器人自己）的 wxid |
| `type` | int | 消息类型（见下表） |

---

## 三、消息类型（type）

| type | 含义 |
|------|------|
| `1` | 文本消息 |
| `2` | 图片消息 |
| `3` | 语音消息 |
| `34` | 视频消息 |
| `43` | 视频（通用） |
| `47` | 表情消息 |
| `49` | 文件/链接消息 |
| `10002` | 系统消息 |

> 注：具体 type 值与微信 SDK 定义有关，以上为常见值。文本消息通常为 `1`。

---

## 四、回调示例

### 收到一条个人文本消息
```json
{
  "fromUser": "wxid_abcd1234",
  "msgId": 16183123456789012,
  "content": "你好",
  "nickname": "张三",
  "wxid": "wxid_robot_self",
  "type": 1
}
```

### 收到一条群消息
```json
{
  "fromUser": "1234567890@chatroom",
  "msgId": 16183123456789013,
  "content": "大家好",
  "nickname": "李四",
  "wxid": "wxid_robot_self",
  "type": 1
}
```

---

## 五、接收方要求

`hook_url` 对应的后端接口需：
- 支持 **POST** + **JSON**；
- 接收上述 6 个字段；
- 建议**快速响应**（本服务 POST 有 10 秒超时，超时/失败会记录日志但不影响监听）。

---

## 六、相关配置

见 `weixin_config.txt`：

| 配置项 | 默认值 | 说明 |
|--------|--------|------|
| `listen_port` | `10878` | 消息监听 socket 端口（一般无需调整）|
| `hook_url` | `http://localhost:8080/stock/wechat/hook` | 消息回调推送地址 |

---

## 七、排查

| 现象 | 原因 | 处理 |
|------|------|------|
| 无回调 | 服务未启动 / 微信未登录 / 注入失败 | 查看 `service.log` |
| POST 失败日志 | `hook_url` 后端不可达 | 确认后端服务已启动、URL 正确 |
| 日志有"跳过自己发送" | 该条消息是自己发的 | 正常，不会回调 |
